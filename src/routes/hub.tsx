// Hub route — terminals are rendered persistently in ClientLayout.
// This component is empty because the dashboard IS the terminal mosaic,
// which stays mounted even when navigating to other pages.
export default function Hub() {
  return null;
}
